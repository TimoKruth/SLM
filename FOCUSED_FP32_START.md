# Focused FP32 comparison started — 15 September 2026

The user authorized the proposed focused comparison. It started **15 September 2026 at 08:11:45 Europe/Berlin**, with a hard deadline of **14:01:33**. Active worktree `/Users/timokruth/Projekte/SLM-focused-optimization`, branch `codex/focused-optimization`, commit `8ab004d`. Detailed frozen design: `FOCUSED_FP32_12000.md` in that worktree.

Authoritative state: `/Users/timokruth/Projekte/SLM-focused-optimization/runs/focused-fp32-12000-2026-09-15/status.json` and `direct-status.json`. Creating `STOP` in that run directory requests a stop. There is no automatic restart, extension, model adoption or subsequent campaign.

Six paired trials compare synchronization every update versus every four updates. All use FP32, the same original 27.3M six-hour parent/Adam checkpoint, three original data orders, batch 2, context 1024 and LR 3e-5. Every successful trial stops at **exactly 12,000 updates**; speed is measured from actual elapsed process time. The 2,600-second per-trial allowance is a safety cap, not the training endpoint. Incomplete targets save a full checkpoint and pause.

Fresh confirmation has **570 tasks from 23 sources**, with 490 answer-correctness tasks across six families. It excludes training groups and 3,772 groups from 34 earlier suites. Dialogue increases from 12 to 34 distinct tasks; only 34 untouched groups remained. Quoref has no untouched groups left and remains in both reused diagnostic suites. The old 384/288-task suites are explicitly diagnostic, not fresh confirmation. There are 19 evaluations including fresh parent quality. Original accuracy, per-family, reference-loss and strict resource/speed gates remain unchanged. No external-transfer or functional-code guarantee.

The new study uses a six-hour total ceiling including preparation: **612 seconds** charged for preparation/launch reserve; **20,988 seconds (5h 49m 48s)** maximum after launch. Required future stage allowances including reserves total 19,560 seconds. No unused budget from earlier completed studies is added. Early completion does not trigger extra work.

59 CPU tests passed. The numerical grouping functions and entire training loop body match the preceding FP32 study; only the opt-in fixed-update stop condition changed. Both real model/Adam/sampler continuation controls and an actual four-update stop/save smoke passed before the fresh parent evaluation. Input hashes and suite separation were checked before GPU work. Mains power, normal thermals and zero swap-counter growth were observed in the prestart interval; the exclusive GPU lease, light monitoring and PowerWatch remain active throughout.
